# ---------------------------------------------------------------------------
# Never managed, on any OS
# ---------------------------------------------------------------------------

# Regenerated by `caelestia scheme` every time the colourscheme changes.
# Tracking these means a dirty worktree after every theme switch.
.config/gtk-3.0
.config/gtk-4.0
.config/qt5ct
.config/qt6ct
.config/qtengine
.config/kdeglobals
.config/btop/themes
.config/kitty/themes
.config/alacritty/themes
.config/nvtop/nvtop.colors
.config/htop/htoprc
.config/hypr/scheme/current.lua

# Plugin lockfile churn is managed by lazy.nvim itself, not by hand.
# (Delete this line if you'd rather pin plugin versions across machines.)
.local/share/nvim

# Runtime / cache state that happens to live under .config
.config/caelestia/monitors

# ---------------------------------------------------------------------------
# Windows: skip everything that only makes sense under Hyprland / zsh
# ---------------------------------------------------------------------------
{{ if ne .chezmoi.os "linux" }}
.zshrc
.p10k.zsh
.config/caelestia
.config/hypr
.config/uwsm
.config/fuzzel
.config/cava
.config/foot
.config/kitty
.config/alacritty
.config/btop
.config/fastfetch
.config/micro
.config/satty
.config/MangoHud
.config/vkBasalt
.config/xsettingsd
.config/mimeapps.list
.config/dolphinrc
.ssh/config
{{ end }}

# ---------------------------------------------------------------------------
# Linux: skip the Windows-only half
# ---------------------------------------------------------------------------
{{ if eq .chezmoi.os "linux" }}
Documents/PowerShell
AppData
{{ end }}

# ---------------------------------------------------------------------------
# Profile-conditional
# ---------------------------------------------------------------------------
{{ if not .gaming }}
.config/MangoHud
.config/vkBasalt
{{ end }}
